import { CategoryPage, categoryMetadata } from "@/lib/category-page";

export const metadata = categoryMetadata("automatic-litter-boxes");

export default function Page() {
  return <CategoryPage slug="automatic-litter-boxes" />;
}
